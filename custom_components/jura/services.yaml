force_update:
  name: Force Update
  description: Force an immediate poll of the machine.
  fields:
    config_entry_id:
      name: Config Entry ID
      description: The config entry ID of the machine (alternative to entity_id).
      selector:
        text:
    entity_id:
      name: Entity
      description: Any Jura sensor entity (alternative to config_entry_id).
      selector:
        entity:
          integration: jura

lock_screen:
  name: Lock Screen
  description: Lock the machine's front-panel display.
  fields:
    config_entry_id:
      name: Config Entry ID
      selector:
        text:
    entity_id:
      name: Entity
      selector:
        entity:
          integration: jura

unlock_screen:
  name: Unlock Screen
  description: Unlock the machine's front-panel display.
  fields:
    config_entry_id:
      name: Config Entry ID
      selector:
        text:
    entity_id:
      name: Entity
      selector:
        entity:
          integration: jura

brew:
  name: Brew
  description: >-
    Brew a drink. Pick a product by name (optionally overriding
    strength/water/temperature/grinder-ratio/milk/milk-foam), or pass a raw
    recipe payload. This PHYSICALLY brews a drink on the machine.
  fields:
    config_entry_id:
      name: Config Entry ID
      selector:
        text:
    entity_id:
      name: Entity
      selector:
        entity:
          integration: jura
    product:
      name: Product
      description: >-
        Product name from the machine's product table (e.g. "espresso").
        See the "Brew Product" select for the valid names on your model.
        Mutually exclusive with recipe.
      example: espresso
      selector:
        text:
    strength:
      name: Coffee strength
      description: >-
        Optional strength level (model-specific, e.g. 1-10). Omit for the
        product's factory default. Clamped to the product's valid range.
      selector:
        number:
          min: 1
          max: 16
          mode: box
    water_ml:
      name: Water
      description: >-
        Optional water amount in millilitres. Omit for the product's factory
        default. Clamped to the product's valid range.
      selector:
        number:
          min: 0
          max: 1000
          unit_of_measurement: mL
          mode: box
    temperature:
      name: Temperature
      description: >-
        Optional temperature level (model-specific, e.g. 1 = Normal, 2 = High).
        Omit for the product's factory default. Clamped to the product's valid set.
      selector:
        number:
          min: 0
          max: 3
          mode: box
    grinder_ratio:
      name: Grinder ratio
      description: >-
        Optional left:right grinder split declared by twin-grinder profiles
        (for example 100_0, 75_25, 50_50, 25_75, or 0_100 on EF566).
        Omit for the product's factory default.
      example: 100_0
      selector:
        text:
    milk_s:
      name: Milk
      description: >-
        Optional milk dispensing time in seconds (JURA machines meter milk by
        pump time, not volume). Omit for the product's factory default.
        Clamped to the product's valid range.
      selector:
        number:
          min: 0
          max: 120
          unit_of_measurement: s
          mode: box
    milk_foam_s:
      name: Milk foam
      description: >-
        Optional milk-foam dispensing time in seconds. Omit for the product's
        factory default. Clamped to the product's valid range.
      selector:
        number:
          min: 0
          max: 120
          unit_of_measurement: s
          mode: box
    recipe:
      name: Recipe (advanced)
      description: >-
        Raw hex payload without the @TP: prefix (legacy/advanced).
        Mutually exclusive with product.
      example: "02FF0809FFFF02FFFFFFFFFFFFFFFFFF"
      selector:
        text:

clean:
  name: Start Cleaning Cycle
  description: Start the coffee-system cleaning cycle (~5 min, requires cleaning tablet, locks machine).
  fields:
    config_entry_id:
      name: Config Entry ID
      selector:
        text:
    entity_id:
      name: Entity
      selector:
        entity:
          integration: jura

descale:
  name: Start Descaling Cycle
  description: Start the descaling cycle (30+ min, requires descaler in tank).
  fields:
    config_entry_id:
      name: Config Entry ID
      selector:
        text:
    entity_id:
      name: Entity
      selector:
        entity:
          integration: jura

filter_change:
  name: Filter Change
  description: Run the water-filter change procedure.
  fields:
    config_entry_id:
      name: Config Entry ID
      selector:
        text:
    entity_id:
      name: Entity
      selector:
        entity:
          integration: jura

cappu_rinse:
  name: Rinse Milk System
  description: Rinse the cappuccino system with hot water.
  fields:
    config_entry_id:
      name: Config Entry ID
      selector:
        text:
    entity_id:
      name: Entity
      selector:
        entity:
          integration: jura

cappu_clean:
  name: Clean Milk System
  description: Run the cappuccino-system cleaning cycle (requires milk-system cleaning tablet).
  fields:
    config_entry_id:
      name: Config Entry ID
      selector:
        text:
    entity_id:
      name: Entity
      selector:
        entity:
          integration: jura

power_off:
  name: Power Off
  description: Put the machine into standby. Re-enabling requires manual wake-up.
  fields:
    config_entry_id:
      name: Config Entry ID
      selector:
        text:
    entity_id:
      name: Entity
      selector:
        entity:
          integration: jura

restart:
  name: Restart WiFi Dongle
  description: Reboot the WiFi dongle. The integration will reconnect automatically.
  fields:
    config_entry_id:
      name: Config Entry ID
      selector:
        text:
    entity_id:
      name: Entity
      selector:
        entity:
          integration: jura
